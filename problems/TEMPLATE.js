const { runProblem, assert } = require('../utils');

// ─── YOUR SOLUTION ───────────────────────────────────
function myFunction(/* params */) {
  // Write your solution here

}
// ─────────────────────────────────────────────────────

runProblem('Problem Title', (assert) => {
  assert('test description',  myFunction(/* input */),  /* expected output */);
  assert('edge case',         myFunction(/* input */),  /* expected output */);
});
